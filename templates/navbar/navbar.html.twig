<nav class="navbar navbar-expand-lg bg-dark bg-gradient">
    <div class="container-fluid">
        <a href="{{ path('app_home') }}"><img class="" src="https://upload.wikimedia.org/wikipedia/commons/thumb/c/c6/Breezeicons-apps-48-steam.svg/48px-Breezeicons-apps-48-steam.svg.png?20190217130159" width="40" alt=""></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-between ms-5" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item me-5">
                    <a class="text-light nav-link active" aria-current="page" href="{{ path('app_home') }}">Games</a>
                </li>
                <li class="nav-item me-5">
                    <a class="text-light nav-link active" aria-current="page" href="#">Library</a>
                </li>

                <li class="nav-item dropdown me-5">
                    <a class="text-light nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Genres
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                        <li><a class="dropdown-item" href="#">FPS</a></li>
                        <li><a class="dropdown-item" href="#">MMO</a></li>
                        <li><a class="dropdown-item" href="#">SIMULATION</a></li>
                    </ul>
                </li>
            </ul>
            <ul class="navbar-nav">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <div class="dropdown">
                        <a class="btn btn-warning dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
                            {{ app.user.email }}
                        </a>

                        <ul class="dropdown-menu bg-light" aria-labelledby="dropdownMenuLink">
                            <li><a class="dropdown-item" href="{{ path('app_user_show', {'id': app.user.id}) }}">Account</a></li>
                            <li><a class="dropdown-item" href="#">Setting</a></li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item text-primary" href="{{ path('app_admin_user_index') }}">Users</a></li>
                                <li><a class="dropdown-item text-primary" href="{{ path('app_admin_game_index') }}">Games</a></li>
                            {% endif %}

                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Logout</a></li>
                        </ul>
                    </div>

                {% else %}
                    <div class="dropdown">
                        <a class="btn btn-warning dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
                            Connexion
                        </a>
                        <ul class="dropdown-menu bg-light" aria-labelledby="dropdownMenuLink">
                            <li><a class="dropdown-item" href="{{ path('app_login') }}">Login</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_register') }}">Register</a></li>
                        </ul>
                    </div>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>